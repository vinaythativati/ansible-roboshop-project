  - name : filters
    hosts : frontend
    vars : 
      topic : "linux ,  shell ,pkg , debing ,centos"
      name : "vinay kumar"
      taMap :
        name : anisble
        project : one
        env : dev
      taslist :
      - { key : "name" , value: "jao" }
      - { key : "nam" , value: "jo" }
      - { key : "nme" , value: "ao" }  
    tasks : 
    - name : string list
      debug :
        msg :  "{{topic | split(',')}} " 
    
    - name : map to list
      debug :
        msg :  "{{ taMap | dict2items }} " 

    - name :  list to map
      debug :
        msg :  "{{ taslist | items2dict }} " 