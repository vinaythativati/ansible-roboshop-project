- name : installing multiple application
  hosts : frontend
  become : yes
  tasks :
  - name : installing multiple package
    package :
      name : "{{ item }}"
      state : present
      loop :
      - nginx
      - mysql
      - mongodb-mongosh
      - git
