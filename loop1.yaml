- name : printing the item
  hosts : frontend
  tasks :
    - name : looping the items
      debug :
        msg : "{{ item }}"
      loop :
        - linux
        - redhat
        - debin
        - centos
  